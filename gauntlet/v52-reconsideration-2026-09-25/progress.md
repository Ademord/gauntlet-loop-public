# 5.2 reconsideration and continuity upgrade

**Published as GitHub release 5.2.0.** [Publication receipt](publication.json) verifies the release commit, remote tag/main and all three uploaded assets. Version 5.2.0 is governed by 5.1.0 at base `3a10854`. [State](state.yaml), [frozen contract](contract.yaml), [events](events.jsonl), [package identities](evidence/candidate-identity.json).

The change connects conceptual dissatisfaction to bounded reconsideration inside the existing loop. Incoming messages retain unfinished work, with immediate stops and scoped corrections. Source changes are in five existing skill files. The optional private feedback intake is context, not a replacement workflow or a background learner.

## Decision being funded

| Question | This round |
| --- | --- |
| Exact claim | The skill and its portable carriers explicitly handle challenged purpose and incoming obligations without weakening budgets, user authority or independent acceptance. |
| Compared with what? | The byte-preserved 5.1.0 source and its existing purpose/stall checks. No native-agent or incumbent execution control was run. |
| Stop rule | Do not release with a blocking frozen check; stop at the recorded review allowance. Source acceptance and generated-prompt acceptance are separate. |
| Where did it lose? | Three material first-round scenario failures, a generated prompt missing key behavior, two unknown-cost timeouts, a remaining overstrong repeat-attendance suggestion, and response-length overruns. |
| Actual cost | **$5.260492 known delegated API-equivalent usage**, plus two unknown-cost timeouts. Lead, preparation, orchestration, subscription cash and human time unknown; pilot costs separate. |
| Next expenditure | Use the version on ordinary work and observe accepted outcomes, genuine corrections, latency and total effort before funding another feature or comparative study. No paid benchmark is scheduled here. |

## Evidence and revisions

Eight requirements and six independently authored prompts were frozen before edits. The two variant prompts produced eight initial next-action responses. The task-specific scoring rubric was withheld from execution workers. [Scenario index](evidence/scenarios/index.json) contains frozen prompts, exact outputs, reconstruction recipes, scoring and failures.

| Stage | Result and consequence |
| --- | --- |
| Design critique | Warned against reinterpretation by consensus. Mandatory interpreter counts and review-index resets were rejected. |
| Source review 1 | Found missing stop propagation and concrete-choice/dissent semantics in standalone carriers; corrected. |
| Scenario round 1 | Invented required failure before checking feasibility; described planned stop as completed; weakened an attendance trade-off. Preserved failures informed a bounded correction. |
| Source review 2 | Compression dropped immediate reconsideration timing; restored the phrase in template and both examples. |
| Four targeted reruns | Corrected the material defects; S1 still overweights avoiding repeats, and several replies exceed requested length. These are one-sample responses, not live actions. |
| Source review 3 | Accepted C1â€“C6 on exact source diffs. Independently rejected the generated prompt for omissions, despite its successful generation. |
| Prompt repair + integration | One feedback-guided revision, inspected separately in [review 4](evidence/reviews/round-4.json). The initial draft is still a failed forward probe; repair does not establish automatic compliance. |

The final source differs from the round-2 behavioral snapshot only in the portable trigger timing and entrypoint compression documented by review 3. Reuse of earlier scenario evidence is limited to unchanged semantics; no claim says the entire final package was rerun on every case.

## Verification

[Final deterministic receipts](evidence/final-validation.json): nine source files, ZIP and portable identities, 16 skill links, three YAML blocks, 37 archived files, research-index integrity and generated views. The entrypoint is 14,450 bytes; portable export 109,983, within the unchanged 16,000/110,000 limits. The existing suite ran 182 tests with one skip and no failures; its [receipt](evidence/tests.json) states its provenance. [Archive comparison](evidence/history-verification.json) checks old hashes, not just freshly regenerated sums. [Publication scan](evidence/public-scan.json) and staged guard report no hard-pattern findings.

The package validator adds two version-gated marker checks; they detect a missing carrier marker, not semantic correctness. Prior 5.1.0 still validates. Original historical release fingerprints were relocated to exact archived copies rather than silently repointed at new bytes.

## Costs and limitations

[Cost ledger](evidence/costs.json): seven charged critic attempts under the hard cap of twelve, including one timed-out design attempt; answer generation and draft construction counted as provider requests. Successful delegated calls returned `claude-opus-5-5`, with `xhigh` configured. Unknown timeout usage prevents claiming the full $8 soft target was observed.

Two [reported pilots](evidence/pilot-observations.json) supplied source/handoff lessons and positive reactions to concepts. They were not reproduced here and lack controlled comparators. Their later checkpoints may be collected in the existing private review-records directory; they do not silently extend this frozen release evidence. No demonstration of fewer corrections, faster delivery, automatic learning or native interruption control is claimed.

The pre-existing `features enabled or none` placeholder conflicts with the always-on evidence ladder. It is disclosed in the release review; the repaired generated record uses `evidence_ladder`. The installed private 5.1.1 customization and active pilots were not overwritten.

Rollback: use the prior published 5.1.0 package or a backed-up local installation. Do not restore source by rewriting historical records. A later installation should follow [INSTALL.md](../../INSTALL.md), including conflict detection.
