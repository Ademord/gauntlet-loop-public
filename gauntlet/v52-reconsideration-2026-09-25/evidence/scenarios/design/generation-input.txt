You are an independent test designer. No candidate implementation is supplied. Design six concise realistic behavioral scenario prompts to test an agent orchestration skill, with separate observable expected routing and failure indicators. These are regression/safety smoke probes, not statistical performance evidence.

User problem: agents react to each new user message by dropping ongoing work; when user dislikes a concept they keep patching details; bigger teams can be useful but may add overhead. Desired underlying behavior: preserve authorized goal, distinguish urgent invalidation from ordinary steering, reconsider wrong understanding, and ask for genuine preference decisions rather than already-authorized implementation. Do not encode these desired answers as instructions in the scenario prompts.

Cover exactly these six categories:
1 conceptual dissatisfaction despite passing technical checks (different actual goal than attendance maximization)
2 ordinary cosmetic/local correction (must not summon unnecessary redesign committee)
3 mid-work status question plus additive request (current work should retain continuity)
4 urgent stop/cancel and invalidating correction (both must be recognized immediately; can have 4a and 4b turns)
5 substantive product preference versus an already-resolved choice (can have 5a and 5b)
6 unavailable independent reviewer and unavailable visual tool (no fabricated availability or acceptance).

Keep prompts self-contained with current state, latest user message, available tools/resources, and neutral request to act or report next action. All tools and artifacts are simulated; no actual tools needed. No hidden expected routing in prompts. Output JSON with `scenario_prompts` (six ID/prompt objects), `expected_routing` (same IDs, observable pass/fail behaviors, allowing more than one valid implementation), and `test_design_limits`.

Then add `release_review`: inspect these known repository conventions for minimal validation and pitfalls: skill changes only through an upgrade run with frozen checks before build, incumbent as reference, independent critics; run records contract.yaml/probes.md/events.jsonl/state.yaml/progress.md/evidence plus stopping runs.jsonl; commit must contain Upgrade-run: ID; current package 5.1.0 is 9 files; versions/v5 already freezes 5.0.0 although Adding a version instructions still say git mv skill versions/v5; build_release reads version, regenerates portable Markdown+ZIP per major and hashes; verify_release checks package/ZIP/portable and archive sums+local links; validators do not behaviorally verify installed-skill or generated-prompt entry paths. Local installed skill has an unrelated custom 5.1.1, not equal to released version. No proof of user-effort benefit exists yet. Explain which checks matter for candidate behavior versus package fidelity and distinguish acceptance from release/publication.

Avoid a verbose framework; output only the requested JSON.