Complete the following repair for the maintainers of this repository: `call_json` in vlm.py has been removed: its body now raises NotImplementedError, and 8 tests fail. Reimplement it so the whole suite passes. Its signature and docstring are unchanged and the tests state the behavior; do not change any test. Local-only; no hosting, no CI. Compare the result against the failing original on behavior, not style: record the pre-fix revision, run `python -m pytest -q -p no:cacheprovider` and keep the output as the reference snapshot. Required checks: the failing tests tests/test_core.py::test_cache_is_a_one_way_ratchet, tests/test_core.py::test_pipeline_extracts_target_offline, tests/test_core.py::test_pipeline_non_target_gets_null_fields, tests/test_core.py::test_pipeline_blank_is_unprocessable_but_status_ok, tests/test_core.py::test_pipeline_provider_crash_is_failed_provider, tests/test_core.py::test_pipeline_garbage_is_failed_parse, tests/test_experiment.py::test_ablation_takes_output_as_is, tests/test_experiment.py::test_ablation_records_parse_failure_without_retry pass; the full suite (python -m pytest -q -p no:cacheprovider) passes with no new failures and no skips; changes stay within vlm.py; regression coverage addresses the failure class, not one example. Reuse existing authorization and project constraints.

Difficulty is medium because 29 lines of removed implementation, not a single token, 8 failing tests pin the behavior, the docstring states intent; the tests state the contract, suite runs in about 4 seconds; required checks are deterministic. Topology: light. One bounded build, then one independent evidence-based review that reruns the checks; no preference comparison loop. A light review that does not accept escalates to compact with the reason recorded. Builders may run focused tests and formatters in isolation; the independent critic inspects the actual artifact and reruns the checks. Inherit configured models and record actual assignments where observable.

Each verdict carries candidate identity, reference snapshot, criteria version, winner ours/bar/none, the biggest gap, all blocking findings, each check as passed/failed/blocked/not applicable, any HOLD, and concrete evidence. Resolve disputed facts by discriminating observations. Acceptance requires ours to win, every required check to pass, and no blocking HOLD.

Budget: 8 critic reviews total, hard; reserve the last 2 for integration and handoff, so converge at 6. All workers share one ledger; splitting or resuming does not refill it. Disclose unobservable usage.

After two repeated unresolved-gap verdicts, diagnose before editing. Checkpoint after each verdict with identities, obligations, holds, owners, next action, and remaining budget; after any pause, record a resume validation decision before dispatching. Keep a readable progress file. At acceptance or budget exhaustion, append one milestone line to gauntlet/runs.jsonl in the v5 milestone shape (difficulty, verifiability, topology, reviews used, advisory-only reviews, outcome state, wall-clock, models) and stop. No benchmark, model sweep, or scheduled follow-up.
