task_id: t002-tpm-demo-autoplay-on-entry
repo: <projects-root>/tinder-power-mode
base_commit: "8b3ca045e6f820adde9b3b1a90db7d9203f374b5"     # parent of the answer commit
answer_commit: "a312ea07d9f7b7c00b7934bd989b345107bbeb28"   # 2026-09-08 "Start the Tinder demo tour automatically on entry"
test_files_from_answer: [test/contracts.test.js]
held_out_files: []
held_out_study_files: []
setup_copy_from_repo: [node_modules]   # jsdom is a devDependency; the runner copies it so the clone runs offline
description: >
  The demo page (docs/index.html, built from demo/src.html by node demo/build.js) should start its guided
  tour automatically when the page is opened, on desktop and on a phone-width visit with saved preferences,
  and pressing the pause/play control must cancel any pending tour key so nothing fires after a pause. Make
  the two failing tests in test/contracts.test.js pass without breaking the suite, and rebuild docs/index.html
  with node demo/build.js so the build check stays green. Local-only; no hosting, no CI.
test_command: npm test --silent
failing_tests:
  - "demo autoplays on fresh desktop visit and pause/resume cancels pending keys"
  - "demo autoplays on phone visit with saved preferences and pause/resume cancels pending keys"
allowed_paths: [demo/src.html, docs/index.html, README.md]
difficulty:
  estimate: low
  proxies: [one demo page, two new tests describing the behavior exactly, no library or interface change, suite runs in about 10 seconds with jsdom]
verifiability: deterministic
task_class: code-feature
review_cap: 8
mint_check_2026_09_18:
  at_answer: "pass 30, fail 0 (node --test with jsdom), about 9.4 s"
  at_base_with_answer_tests: "2 failing tests, exactly failing_tests"
  answer_code_diff: "demo/src.html +3/-2, docs/index.html +3/-2 (built copy), README.md +2/-2"
  suite_runtime_s: 9.4
  dependencies: "jsdom 24.1.3 from the source repository's node_modules (copied by setup_copy_from_repo); node v24"
held_out_intent: >
  None yet. A held-out at a tablet width (1024) would separate a genuine autoplay from a width-gated hack,
  but it needs the test file's private setup helper; write it when the pair is scheduled.
