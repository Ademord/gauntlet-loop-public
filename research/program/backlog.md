# Program backlog

Proposals and follow-ups for the Gauntlet skill and its research program. A row here is a candidate, not an authorization. Skill changes follow the bounded workflow-improvement process in the learning reference (source failure, hypothesis, intended effect, permitted files, patch, rollback) and land only on a versioned branch with a snapshot of the incumbent and an `UPGRADES.md` entry, approved by the owner. Statuses: `candidate | drafted | reviewed | adopted | rejected | superseded | deferred`.

| ID | Title | Kind | Source | Gate | Status |
| --- | --- | --- | --- | --- | --- |
| B-001 | Run ledger phase 0: one `runs.jsonl` schema, `tools/ledger/ingest.py`, `tools/ledger/features.py` with an `insufficient` marker below three runs per arm and a self-report mismatch column, a denominator of unrecorded `gauntlet/` directories, a monthly human-read report. Not built: the lesson tally (no lesson files in the v5 schema exist yet) and a crawlable manifest | tooling, zero model tokens | two commissioned pitches, 2026-09-17 (private) | none | adopted 2026-09-18 (3 recorded runs, 5 unrecorded directories; outputs private) |
| B-002 | Add `task_class` (closed vocabulary) and `features_enabled` (closed vocabulary of v5 toggles) to the milestone summary in the execution contract, and to the prompt-drafting include list | skill change (5.1 candidate) | architect pitch | B-001 schema agreed | candidate |
| B-003 | Guard: a pre-commit or CI check that rejects commits touching `skill/` whose author or message marks them as automated; proposals may only land under a proposals directory | tooling | skeptic pitch | none | adopted 2026-09-19 as a commit-msg hook: any commit that changes `skill/` must carry `Upgrade-run: <run-id>` naming an existing run folder (stronger than the original author-marker idea) |
| B-004 | Migrate the two pre-v5 `runs.jsonl` rows to the phase-0 schema with `unknown` fills; record the v5 run as a row | data | architect pitch | B-001 | adopted 2026-09-18 |
| B-005 | Rule-based proposal candidates (`tools/ledger/propose.py`) writing to a proposals backlog with the learning reference's fields; one bounded model call per drafted proposal, owner-approved | phase 1 | architect pitch | B-001 running for at least one month | deferred |
| B-006 | Rendering of the feature-delta cells (a config generator from the ledger for a flow-diagram library); the run tree as a separate small SVG from git log | visualization | architect pitch | at least one ledger cell not `insufficient` | deferred |
| B-007 | Paired study of one toggle on held-out tasks with a rubric written first (research program phase 2). Owner answered 2026-09-17: willing to pay where there is value. Protocol pre-registered (`phase2-paired-study.md`), harness built and self-tested (`tools/paired_study/`), two tasks minted and checked (`paired-study/tasks/`). Next: the first pair on t001 with flag F1 as the harness test | designed experiment, paid | research program section 7; owner's answer | first pair: owner's go; full 30 pairs: mutation-minted supply (B-014) | first look taken 2026-09-19 at 14 pairs: no difference detected under the pre-registered tests; light used about a third fewer tokens (sign-test p 0.057, above the per-look threshold); continuing toward the planned 30 pairs |
| B-008 | Contextual-bandit prioritization over toggles (research program phase 3) | learner | architect pitch | B-007 shows a gradient | deferred |
| B-009 | v4 inheritances flagged by the prompt-mode critic: the Draft and Execute route verbs both match "make a prompt for: fix X" (state that the outer request governs); `winner: bar` reads oddly for a repair against a failing original (consider a `repaired` value or a note in the verdict record) | skill change (5.1 candidate) | R3 verdict, run v5-upgrade-2026-09-17 | none | candidate |
| B-010 | Install v5.0.0 into a live skills directory with a before-and-after hash check, after the owner reviews the branch | installation | this run | owner decision | candidate |
| B-011 | Read the six full papers behind the v5 research basis and record any qualification the abstracts hid; revise limits accordingly | research | thesis section 8 | none | candidate |
| B-012 | Prompt template length: at 370 words with 11 slots, a faithful adaptation lands above the 250-450 target and needs a compression pass (the reference now says to compress); decide whether to raise the target to 300-500 or cut the template to about 300 words | skill change (5.1 candidate) | R3 and R5 verdicts | none | candidate |
| B-013 | Budget rule edge cases under small caps: at H = 8 the per-piece soft and hard limits coincide (no convergence window); state whether that is intended or whether small caps should drop the soft point | skill change (5.1 candidate) | R6 verdict | none | candidate |
| B-014 | Mutation-minted tasks: a script that takes an offline-passing repository, applies one semantic mutation to a test-covered function, confirms exactly one or two tests fail, and writes a task spec with the original as the answer; supply for 30 F1 pairs. Limit: low-difficulty class only; cannot exercise F2 | tooling, zero model tokens | task-pool ranking 2026-09-18 (private) | B-007 first pair proves the harness | adopted 2026-09-18, extended 2026-09-19: 12 tasks minted and used; a second batch is being minted to reach 30 pairs; the script no longer re-mints a source line |
| B-015 | Recording discipline: write v5-shape milestone lines for the five unrecorded gauntlet directories in the author's workspace from their prose logs where the facts are recoverable, marking everything else `unknown`; the ledger denominator shows them until then | data | ledger report 2026-09 (private) | none | candidate |
| B-016 | Harness execute mode: run the first pair (t001, F1) headlessly, read both transcripts, confirm the results row and the held-out verify, and record the measured per-arm cost that replaces the ASSUMPTION in the protocol | experiment, paid (two small runs) | B-007 | owner's go | done 2026-09-19: the harness test ran, its transcripts were read, two harness defects were found and fixed, and the measured cost replaced the estimate (about two dollars and seven minutes per pair) |
| B-017 | Held-out test for t002 at tablet width, once the test file's private setup helper can be reused | data | task-pool ranking 2026-09-18 (private) | none | candidate |
| B-019 | Every interaction as an experiment: the owner's framing (18 September 2026) that each chat with a coding agent should behave like a reinforcement-learning step, backtracking and updating the best policies, with some chats activating more of the skill's features than others, so the loop improves from daily use. Program section 9 (every task is an experiment) is the observational half; the missing halves are a per-chat feature-activation record and a policy-update rule that only the owner can approve. Design note, not a build | research direction | owner, 2026-09-18 | B-007 data exists | candidate |
| B-020 | Restore what v4 dropped: the bars-by-goal-type table, two short filled examples, and the list of what breaks a loop. v1 to v3 carried them; v4 and v5 do not, a loss in teachability nobody measured. Candidate for 5.1, in the entrypoint if the size budget allows or as a reference loaded only for prompt drafting | skill change (5.1 candidate) | version chronology review, 2026-09-18 | a size decision (the entrypoint is 11 bytes under 16,000) | candidate |
| B-021 | Environment model: an optional per-project record of `action -> precondition -> observed effect` with the environment identity and a last-confirmed date, written only after a run is surprised twice by the same mechanism, revised or retired in place on contradiction | skill change (5.2 candidate) | scan 2026-09-19, Continual Enterprise World Model Discovery | a project where the same environment surprise recurs | candidate |
| B-022 | Tool surface: record the available tool names and their source in the run contract; require every evidence pointer naming a command, path, or URL to resolve when a critic records it (otherwise the check is `blocked`); record the resolved tool list and refused or unknown-tool attempts per paired-study arm | skill change (5.1 candidate) plus harness | scan 2026-09-19, Closed-World Resolution | none | candidate |
| B-023 | Checkpoint handoff: split an arm's outcome into arriving at a usable state and finishing from one, by cloning each arm's first-verdict state and crossing the solvers; design in [checkpoint-handoff.md](checkpoint-handoff.md) | program design, paid when run | scan 2026-09-19, Reach or Solve? | after the first F1 look; needs tasks long enough to have a milestone (B-025) | design revised 2026-09-19 against the full text; gated on B-028 |
| B-024 | Behavioral acceptance for deliverables that act: a selectable check class in the software-quality reference requiring observed outcomes across at least two environment conditions for products that price, bid, schedule, or allocate, with the agent's stated reasoning recorded as `judgment` context and never as evidence | skill change (5.1 candidate) | scan 2026-09-19, Faithful yet Collusive | none | candidate |
| B-025 | Medium tasks, hand-cut: cut a handful of tasks by hand from release-sized commits, big enough to reach a third review round and to have a milestone state, so F2 (the evidence ladder) and the checkpoint handoff have a population | data | task-pool ranking 2026-09-18; checkpoint-handoff design | none | candidate |
| B-026 | Read the full texts of the four 19 September papers and record any qualification the abstracts hid, as B-011 does for the six behind v5 | research | scans 2026-09-19 | none | in progress: the full text of "Reach or Solve?" was read 2026-09-19 and its note now carries what the abstract hid, including the two handoff variants, the quantified sign flip and the authors' own limit on that diagnostic; three papers left |
| B-027 | Excision task class: remove a whole function body, keep signature and docstring, keep the task only if at least three tests fail. Minter written (`tools/paired_study/mint_excisions.py`); needs excision support in `run_pair.py`, which must not be touched while a series is running. Rationale and limits in [task-classes.md](paired-study/task-classes.md) | harness | first look at F1 | the F1 mutation series finishing | queued |
| B-028 | Excision pilot: up to eight pairs on flag F1, whose only question is the contrast-fired rate. Zero means the class is no better than mutation for F1 and only hand-cut tasks remain; one in eight or more registers a fresh thirty-pair series. Pairs are never pooled with the mutation series | study | first look at F1 | B-027 | queued |
| B-029 | Contrast-fired observable for F2, F3 and F4, decided and written into the protocol before each of those flags runs, on the F1 precedent: a flag whose mechanism never engages produces a real number about a false subject. Table drafted in [task-classes.md](paired-study/task-classes.md) | study | first look at F1 | none | candidate |

The skeptic pitch's question (are you willing to run the same task twice with one flag flipped, roughly doubling the cost of about thirty runs, to learn anything causal about one feature?) was answered by the owner on 2026-09-17: yes, where there is value. B-007 and B-008 therefore stay; B-008 remains gated on B-007 showing a gradient.
