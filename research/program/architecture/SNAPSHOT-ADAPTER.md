# Attributed job snapshots and recovery

**P003, 2026-09-21.** [Indexed result](P003-snapshot-adapter.json), following [P002](IMPLEMENTATION.md). P002's original results and policy boundary remain historical.

The accepted private package 1.1.5 adds a read-only adapter over the actual job index, legacy ledger, package and fixed artifact paths. It supplies the planner with attributable observations and explicit unknown states. Independent review preceded execution; **26/27 corrected acceptance test methods passed; 1 skipped**, and the installed package hash check covered 31 files.

The separate real-source probe checked **2 jobs in 4 inspections**. Local history routed to reconciliation; a current captured singleton-tracker status routed to already submitted with no action. All expected routes matched, and 253 local source files retained the same bytes. These observations do not demonstrate that a new application is ready or that browser execution works.

| Investor question | This increment's answer |
| --- | --- |
| What exact claim? | The adapter passes the specified source-binding and recovery checks, and the selected real-source routes agree with captured tracker status. |
| Compared with what? | P002 accepted supplied snapshots. P003 reads existing sources and narrow observations. There is no equally equipped comparison arm and no measured orchestration advantage. |
| What would make us stop? | One build and at most one repair; reject a failing candidate. Build limit $4/900 seconds and optional repair $2/600 seconds; dollar limits are soft. Actual dispatch limits are recorded per attempt. |
| Where did it lose? | Acceptance rejections: 1. Native budget stops: 1. Controller prelaunch failures: 1. Legacy success still lacks fresh stage evidence. The live probes cover historical jobs, not a complete new application or an executor. |
| What did it really cost? | **$5.471788 CLI-reported API-equivalent execution usage**, 1,460,850 native tokens; controller-to-worker repair requests: 1. All launched P003 attempts are included; reused P002 qualification is not charged again. Cash billing, preparation, evaluation and human time remain unknown. |
| What does the next expenditure enable? | A decision about qualified source capture and recovery evidence for incomplete jobs, before any effect executor. More dispatches alone are not a milestone. |

The adapter never upgrades legacy `ok` rows to fresh artifact/input bindings. Current hashes establish what was read; they do not reconstruct a completed stage's history. Unknown submission or form facts remain unknown. Historical H6/H7 activity stays a duplicate-risk signal unless fresh explicit negative reconciliation binds the complete current ledger and covers the latest recorded activity. Contradictory observations route to reconciliation. The amended policy also handles H6/H7 failed or unknown markers before proposing prefill; this does not rewrite P002's earlier policy result.

Receipts bind controller assertions to captured bytes and timestamps; they do not authenticate a source, prove semantic extraction or grant submission authority. Existing review remains after prefilling. No submit action, tracker mutation, browser executor or general scheduler is added. No legacy file is rewritten by inspection. The controller's preplanned manifest packaging is separate from worker repairs.

The supplementary real-source probe remains outside the frozen synthetic score. User-confirmed resolution and full research cost are unknown. P002 and P003 performance benefits remain **unproven**, MAQ-001 remains unqualified, and no research milestone or general execution capability is promoted.

All 5 supplementary regression methods passed after repair. The same probes reproduced the initial candidate's ancestry, aggregate-budget and H7-only recovery defects. These targeted checks were authored after those defects were found; mocked filesystem boundaries are not native isolation qualification, and these results are separate from the frozen suite and any comparative performance claim.

Controller preparation failures are retained with their raw receipts. Their verified prelaunch incidents made no native model calls and enrolled no worker; they are neither paid model attempts nor worker corrections. Controller preparation cost remains unknown.

The initial frozen suite recorded 25 passes, one invalid CLI fixture failure and one host symlink skip. The test omitted the contract-required intent flag. The original suite and result remain unchanged; a separately frozen correction adds that flag for repair acceptance. Independent review found real ancestry, aggregate-read-budget, recovery and documentation defects that justified the repair regardless of the fixture error. Suite results alone would not have established contract compliance.
