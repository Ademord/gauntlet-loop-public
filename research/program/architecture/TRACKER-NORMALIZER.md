# P004: captured tracker normalization

**Rejected after one build and one repair**, 2026-09-21. [Indexed result](P004-tracker-normalizer.json). No production integration occurred: the private pipeline remains at **1.1.5**. P003 and its original outcomes remain unchanged.

The candidate handles the tested tracker transformations, but still rejects a valid compact aware ISO timestamp form required by the frozen contract. The remaining compatibility defect prevents acceptance. Passing selected cases does not establish full delivery.

Final frozen behavioral checks: **19/19 test methods passed; 0 skipped**. The artifact is nevertheless **rejected** by independent contract review and a failing supplementary ISO case. Separate candidate-only historical diagnostics covered **2 captured jobs**, **2 normalizations** and **4 adapter inspections** using the unchanged installed 1.1.5 adapter. Generated receipts exactly matched the independently reviewed P003 mappings. At the original observation instant the route remained already submitted; after expiry it required reconciliation. Source bytes remained unchanged. These selected diagnostic successes do not override the failure or constitute live tracker evidence.

The initial artifact was rejected despite 19/19 frozen test methods passing: 4 independent review blockers covered incomplete delivery, missing worker validation and unsupported aware ISO forms. The separate two-case ISO probe passed 0/2 initially and 1/2 after repair. Frozen scores and initial failures remain unchanged.

A separate controller candidate-only adapter probe inspected **one incomplete job** using a fresh-at-inspection tracker capture: TODO still left submission unknown and routed to checking submission. Separate controller browser checks found **two saved employer endpoints unavailable**; one employer search did not recover the first selected requisition. The second job received only a browser endpoint check. The worker/runtime performed no browser work. No login, personal data, application form or submission occurred. These observations do not distinguish job closure, moved URLs or bad prior links, and prior submission remains unknown. Refreshing authoritative links and availability takes priority; no additional infrastructure is justified by these observations alone.

Counts: **2 native attempts**; controller repair requests: **1**; native failures: **1**. Both artifact outcomes were rejected. Native completion and artifact acceptance are separate: the budget-exhausted build remains a failed native attempt, and a completed repair does not establish a correct artifact.

| Investor question | Result or decision |
| --- | --- |
| Exact claim? | Full compatibility with the frozen normalization contract. The final candidate does not satisfy it. |
| Compared with what? | P003's careful exact-link mapping, checked for equality. No performance comparison. |
| What would stop the work? | The one permitted repair still failed the contract. This round stops without installation or a third repair. |
| Where did it lose? | Initial delivery had four review blockers; valid compact aware ISO remains unsupported after repair. Frozen tests missed that case. Both artifact failures are retained. |
| Actual cost? | $2.5296575 CLI-reported API-equivalent execution usage, including every launched attempt. Cash charges, preparation, evaluation and human time remain unknown. |
| What does spending next enable? | A later explicitly scoped compatibility correction could address the retained failure. No new infrastructure or contract narrowing is justified, and this round is stopped. |

The existing static validator was reused without a new qualification model call or another recorder. Its earlier usage is not charged to P004 again. Captured bytes do not authenticate their source or timestamp. General sandbox qualification remains blocked; no comparative performance gain or milestone promotion is claimed.
