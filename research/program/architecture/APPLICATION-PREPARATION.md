# P005 — Real application preparation

One source-grounded application preparation was accepted after one build and one repair. The final output is a one-page letter, a two-page saved advert and private requirement/evidence notes. This is a delivery-readiness result on one task. No orchestration-performance gain or research milestone is established. [Indexed record](P005-application-preparation.json).

| Investor question | Recorded answer |
| --- | --- |
| What exact claim? | Can the bounded worker produce grounded data and candid fit notes that pass independent content and finished-document checks? Accepted after one repair on this task. |
| Compared with what? | A capable agent with the same verified sources, templates and working tools is a competent alternative. No comparison arm was run. |
| What makes us stop? | One build (soft $2 / 600 seconds), at most one repair (soft $1 / 300 seconds). A failing final artifact is rejected; criteria are not relaxed. |
| Where did it lose? | The initial letter contained a German case error; one combined evidence row was marked supported despite acknowledged gaps. Static JSON validation accepted both files because it checks syntax, not those judgments. |
| What did it cost? | Build $1.664514; repair $0.2941715; total **$1.9586855 API-equivalent execution usage**, including the failed draft. 336,367 tokens across all reported categories; 329.344 seconds of native execution. Preparation, controller/evaluator usage, cash billing and human time remain unknown. |
| What does the next expenditure enable? | Continue the chosen application after sign-in and prior-submission reconciliation. Reuse the checked render path on the next task and measure preparation overhead before funding more infrastructure. |

The worker used Opus 5 with xhigh configured, ordinary file tools and the already qualified strict JSON validator. It produced two JSON files, then validated their final hashes. Independent review rejected the first version and the single repair fixed the two defects without a broader rewrite. Both versions, their validation receipts and actual usage are retained privately. One controller correction request is recorded; it is not a measured human-correction rate or user-confirmed resolution.

Controller preparation had its own failures. Three environment checks failed, including an application-control block in the canonical Python render route. A prelaunch, additive amendment allowed the unchanged canonical HTML, templates, signature, CSS and PDF options to use an existing Node/Playwright-to-Edge transport. Synthetic probes and all actual document pages were read back and visually inspected. The original Python preflight remains failed; neither the complete pipeline nor a general worker sandbox is qualified by this result.

The controller also corrected its prompt/schema mismatch before dispatch and a duplicated capture label in the first advert PDF. Full letter comparison detected one extractor newline inside a fixed hyphenated compound; an exact, visually verified join restored complete text equality. Original failed checks and copies remain in the evidence. These are controller preparation/review issues, separate from the worker's two content defects. The volume of preparation is a cost concern, not evidence that orchestration is faster.

The documents are prepared, but form access still requires sign-in. No personal data was entered, uploaded or submitted. A local/tracker search found no exact matching record; that absence does not prove no earlier application exists. User resolution remains unknown.

P003 stays accepted at private package 1.1.5; P004 stays rejected and uninstalled. No production skill, accepted pipeline code, research milestone, framework or daily automation changed. Public evidence consists of selected summaries and hashes; private sources and application artifacts are not published.
